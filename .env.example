# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริงจาก Supabase
# (Supabase Dashboard > Project Settings > API)
#
# ค่าทั้งสองนี้เป็น "public" ออกแบบมาให้ฝังใน frontend ได้ ปลอดภัยเพราะมี RLS ป้องกัน
#
# ถ้าไม่ใส่ค่าเหล่านี้ แอปจะทำงานแบบ mock (ข้อมูลอยู่ในหน่วยความจำ)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
